all:
  children:
    admin:
      hosts:
        obs-admin:
          ansible_host: 10.50.0.10
    kubernetes:
      children:
        control_plane:
          hosts:
            k8s-cp1:
              ansible_host: 10.50.0.11
        workers:
          hosts:
            k8s-worker1:
              ansible_host: 10.50.0.12
            k8s-worker2:
              ansible_host: 10.50.0.13
