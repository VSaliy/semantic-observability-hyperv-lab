# Semantic search schemas

Milestone 1 uses JSON Schema for normalized operational events because the first vertical slice needs a simple, language-neutral contract for Python, Java, Docker Compose, and CI validation.
